<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.TabLoginRegister" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.TabLoginRegister" parent="Base.Theme.TabLoginRegister" />

    <style name="Base.Theme.TabLoginRegister.ActionBar" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/blue</item>
        <item name="tabBackground">@color/white</item>
        <item name="titleTextColor">@color/white</item>
        <item name="actionBarStyle">@style/ActionBarStyle</item>
        <item name="android:fontFamily">@font/poppins_regular</item>
    </style>

    <style name="ActionBarStyle" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">@color/blue</item> <!-- Latar belakang action bar -->
        <item name="titleTextColor">@color/white</item> <!-- Warna teks action bar -->
    </style>



</resources>